import lottie from '@/assets/meeting.json';
import Header from '@/components/Header';

export default function Meetings() {
  return <Header heading="Meetings" description="<NEED DESCRIPTION>" lottie={lottie} />;
}
